{% extends 'base.html.twig' %}

{% block title %}Email d'initialisation du mot de passe envoyé !{% endblock %}

{% block body %}

    <div class="class container-fluid">

        <div class="row">

            <div class="col-12 col-md-4 offset-md-4">

        <p class="mt-3">
            Si un compte correspond à votre email, un email vient juste de vous être envoyé contenant un lien permettant d'initialiser votre mot de passe.
            Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
        </p>
        <p>Si vous n'avez pas reçu d'email, vérifier vos spams ou <a href="{{ path('app_forgot_password_request') }}">Essayer à nouveau</a>.</p>
            </div>
        </div>
    </div>

{% endblock %}
